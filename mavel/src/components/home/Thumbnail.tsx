import { Center, Heading } from "@chakra-ui/react";

interface IProps {
    name: string;
    imagePath: string;
    imageExtension: string;
}

export default function Thumbnail() {
    return (
        <Center>
            <Heading>HI</Heading>
        </Center>
    );
}
